{
  "name": "digital-twin-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "cd digital-twin && npm install && npm run dev",
    "build": "cd digital-twin && npm install && npm run build",
    "start": "cd digital-twin && npm run start"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.4.0",
    "next": "16.1.6",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@types/pg": "^8.16.0"
  }
}
